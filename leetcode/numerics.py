import typing

# Fill out the following functions:
def digits(x: int) -> typing.Tuple[int]:
    """
    Given an integer, returns the digits of the integer
    
    assert digits(1234) == [1, 2, 3, 4]
    assert digits(490) == [4, 9, 0]
    """

def is_prime(x: int) -> bool:
    """
    Given a number, indicates whether number is prime or not
    assert is_prime(7) == True
    assert is_prime(4) == False
    """


def product(x: int) -> int:
    """
    Given a number find product of its digits
    assert product(2) == 2
    assert product(14) == 4
    assert product(326) == 36
    """

def factorial(x: int) -> int:
    """
    Given a number find its factorial
    assert product(1) == 1
    assert product(2) == 2
    assert product(5) == 120
    """


